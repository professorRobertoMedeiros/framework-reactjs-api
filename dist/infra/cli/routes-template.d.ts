/**
 * Função auxiliar para gerar o template de rotas
 * @param modelName Nome do modelo
 * @returns Template de arquivo de rotas
 */
export declare function generateRoutesTemplate(modelName: string): string;
